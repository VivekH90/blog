document.addEventListener("DOMContentLoaded",()=>{document.querySelectorAll("#document>section").forEach((s,i)=>{s.id=`section-${i+1}`;const h=document.createElement("h2");h.className="section-title";h.textContent=`${i+1} ${s.getAttribute("title")||""}`;s.prepend(h);s.querySelectorAll(":scope > subsection").forEach((sub,j)=>{const n=`${i+1}.${j+1}`;sub.id=`section-${n.replace(".","-")}`;const sh=document.createElement("h3");sh.className="subsection-title";sh.textContent=`${n} ${sub.getAttribute("title")||""}`;sub.prepend(sh)})})});